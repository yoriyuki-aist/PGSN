((lambda x. goal(x,
    strat("strategy1", {
        goal("goal1", ev("evdence1")),
        goal("goal2",
        strat("strategy2", {
            goal("goal21", ev("evdence21")),
            goal("goal22", ev("evidence22")),
            goal("goal23", ev("evidence23"))
            })),
        goal("goal3", ev("evidence3"))
        }
    )
)) "goal0")